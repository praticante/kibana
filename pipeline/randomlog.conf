input {
    file {
        path => "/var/shared/randomlog.log"
        start_position => beginning
	}
}

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "elastic"
		password => "secret"
	}
}
